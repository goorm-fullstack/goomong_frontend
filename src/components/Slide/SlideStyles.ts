import styled from 'styled-components';

export const Slide = styled.div`
  .slide-container {
    display: flex;
    position: relative;
    height: 354px;
    overflow: hidden;
    width: 100%;
  }
`;
